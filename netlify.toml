[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Vue Router history mode — serve index.html for client routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
